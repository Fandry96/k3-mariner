## 2025-05-18 - Zero-Config Guidance
**Learning:** For apps requiring external keys (like Gemini API), users often abandon the flow if they don't know where to get the key. Adding a direct link in the input tooltip is a critical "Zero-Config" enabler.
**Action:** Always include a `help` tooltip with a direct URL for any API key input field.
