## 2024-05-23 - API Key Onboarding Friction
**Learning:** Users often struggle to find where to get API keys, causing drop-off during onboarding. Streamlit's `help` parameter is a low-effort, high-value way to provide this context inline without cluttering the UI.
**Action:** Always verify if an API key input has a corresponding helper tooltip with a direct link.
